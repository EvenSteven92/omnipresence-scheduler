/**
 * Canonical create CTAs — every surface uses these words.
 *
 * - Card = one upload / atomic content unit (compose, schedule, publish)
 * - Event = real-world ministry moment (Sunday service, worship night, …)
 */
export const CREATE = {
  card: "New card",
  event: "New event",
} as const;
