# Optional AI (TanStack /api/ai/generate via Vercel AI SDK):
# AI_GATEWAY_API_KEY=
# AI_GATEWAY_MODEL=xai/grok-4.1-fast-non-reasoning

# Local worker (Phase 1) — OAuth secrets usually live in worker/.env
# OMNI_WORKER_URL=http://127.0.0.1:8787
